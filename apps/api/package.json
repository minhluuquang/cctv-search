{
  "name": "@cctv-search/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn cctv_search.api:app --reload --host 0.0.0.0 --port 8000",
    "build": "echo 'No build step for Python API'",
    "lint": "ruff check .",
    "format": "ruff format .",
    "typecheck": "echo 'No typecheck for Python'",
    "test": "pytest",
    "clean": "rm -rf .pytest_cache .ruff_cache"
  }
}
